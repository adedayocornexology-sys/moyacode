# Copy this file to ".env" and fill in your real values.
# The server reads these at startup (see main.py).

# Your Supabase project URL and a publishable/anon API key.
SUPABASE_URL=https://bzlchdijdpjjobemrcci.supabase.co
SUPABASE_KEY=sb_publishable_qm4IIrY9y-Hal7LVBqBW8Q_a8eyO708

# Claude API key — powers the in-page assistant "Moya" (free-text chat).
# Optional: without it, the "continue where you left off" flow still works;
# only conversational chat is disabled. Get one at console.anthropic.com.
ANTHROPIC_API_KEY=
# Optional model override (defaults to a fast, low-cost model).
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
